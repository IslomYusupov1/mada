import { CurrencyNewPipe } from './currency-new.pipe';

describe('CurrencyNewPipe', () => {
  it('create an instance', () => {
    const pipe = new CurrencyNewPipe();
    expect(pipe).toBeTruthy();
  });
});
